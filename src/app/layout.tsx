import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Furniture Website",
  description:
    "Discover stylish and affordable furniture for your home. Shop sofas, chairs, tables, and more with fast delivery and great deals.",
  authors: [{ name: "Niwanthi Kaushalya" }],
  openGraph: {
    title: "Furniture E-Commerce Website",
    description:
      "Shop high-quality furniture that blends comfort, style, and affordability.",
    siteName: "Furniture Website",
    locale: "en_US",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-gray-50 text-gray-900`}
      >
        {children}
      </body>
    </html>
  );
}
